# two-sum-learning-notes
力扣hot100——Two Sum 问题学习笔记

[![C++](https://img.shields.io/badge/C++-17-blue)](https://isocpp.org/)
[![LeetCode](https://img.shields.io/badge/LeetCode-Easy-green)](https://leetcode.com/problems/two-sum/)
[![License](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)

### 日期：2025.12.24

> ### 有人相爱，有人夜里开车看海，有人leetcode第一题都做不出来

## 📚 目录

- [问题描述](#问题描述)
- [算法分析](#算法分析)
- [解决方案](#解决方案)
- [C++ 关键概念解析](#学习路线)
- [补充](#补充)

## 🎯 问题描述

给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 **和为目标值** `target` 的那 **两个** 整数，并返回它们的数组下标。

**要求**：

- 每种输入只对应一个答案
- 不能重复使用相同元素
- 可以按任意顺序返回答案

**示例：**

```cpp
输入：nums = [2,7,11,15], target = 9
输出：[0,1]
解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1]
```

## 🚀 解决方案

> ### 哈希表解法（最优）

```cpp
#include <vector>
#include <unordered_map>
using namespace std;

class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int, int> num_map; // 存储数字和索引的映射
  
        for (int i = 0; i < nums.size(); i++) {
            int complement = target - nums[i];
  
            if (num_map.find(complement) != num_map.end()) {
                return {num_map[complement], i};
            }
  
            num_map[nums[i]] = i;
        }
  
        return {}; // 根据题目假设，不会执行到这里
    }
};
```

## 📊 算法分析

时间复杂度：O(n)，只需要遍历一次数组

空间复杂度：O(n)，最坏情况下需要存储所有元素的哈希映射

关键点：使用哈希表实现 O(1) 时间查找

## 📝 C++ 关键概念解析

### 1. vector（动态数组）

vector 是 C++ 中的动态数组容器，可以自动调整大小：

```cpp
// 创建 vector
vector<int> nums = {2, 7, 11, 15};

// 常用操作
nums.push_back(5);      // 添加元素
int size = nums.size();  // 获取大小
int first = nums[0];     // 访问元素
```

---

### 2. 引用传递（& 符号）

**为什么使用 `vector<int>& nums` 而不是 `vector<int> nums`？**

#### 先理解一个核心概念：**传值 vs 传引用**

#### 情况1：不加 `&`（传值）

```cpp
void changeValue(int x) {
    x = 100;  // 修改的是副本，不是原来的值
}

int main() {
    int num = 5;
    changeValue(num);
    cout << num;  // 输出还是 5，没有改变！
}
```

**相当于**：我给你一张我的照片（副本），你在照片上涂改，我的脸不会真的变脏。

#### 情况2：加 `&`（传引用）

```cpp
void changeValue(int &x) {  // 注意这里的 &
    x = 100;  // 修改的是原来的值
}

int main() {
    int num = 5;
    changeValue(num);
    cout << num;  // 输出 100，真的改变了！
}
```

**相当于**：我告诉你我的家庭地址，你来我家门口画了一幅画，这样我的房子就真的改变了。[](https://)


| 传递方式       | 特点           | 内存使用           | 效率 |
| -------------- | -------------- | ------------------ | ---- |
| 传值（不加 &） | 创建完整副本   | 高（复制所有数据） | 低   |
| 传引用（加 &） | 直接操作原数据 | 低（不复制）       | 高   |

**比喻理解**：

* 不加 `&`：给你一本书的完整复印件
* 加 `&`：告诉你图书馆的地址，直接去那里看

---

### 3. unordered\_map（哈希表）

* 提供 O(1) 平均时间复杂度的查找
* 存储键值对（key-value pairs）
* 比普通 `map` 更快，但不保证顺序

#### `num_map.find(complement)`

##### 作用：在哈希表中查找键为 ` complement` 的元素

返回值：

如果找到了：返回一个指向该元素的迭代器（可以理解为一个"指针"或"位置标记"）

如果没找到：返回 num_map.end()

#### ` num_map.end()`

##### 作用：返回一个指向哈希表末尾之后的迭代器

含义：这是一个特殊的标记，表示"不存在"或"查找结束"

### 在 Two Sum 中的具体应用

```cpp
unordered_map<int, int> num_map;  // 存储：数字 → 索引

for (int i = 0; i < nums.size(); i++) {
    int complement = target - nums[i];  // 需要找的数字
  
    // 检查这个数字是否已经在哈希表中
    if (num_map.find(complement) != num_map.end()) {
        // 找到了！返回两个索引
        return {num_map[complement], i};
    }
  
    // 没找到，把当前数字存入哈希表
    num_map[nums[i]] = i;
}
```

### 逐步执行过程：

假设 `nums = [2, 7, 11, 15]`, `target = 9`

1. **i=0**: `nums[0] = 2`

   * `complement = 9 - 2 = 7`
   * `num_map.find(7)` → 没找到（`num_map` 还是空的）
   * 存入：`num_map[2] = 0`
2. **i=1**: `nums[1] = 7`

   * `complement = 9 - 7 = 2`
   * `num_map.find(2)` → **找到了！**（2在哈希表中，索引为0）
   * 返回：`{num_map[2], 1}` = `{0, 1}

### 对比不同查找方式


| 方法              | 作用                   | 是否改变哈希表 | 推荐场景           |
| ----------------- | ---------------------- | -------------- | ------------------ |
| `find(key)`       | 查找键                 | 否（只读）     | **常用**，安全查找 |
| `count(key)`      | 统计键出现次数（0或1） | 否             | 只需知道是否存在   |
| `at(key)`         | 访问值                 | 否             | 知道键存在时访问   |
| `operator[](key)` | 访问或创建键           | 是             | 需要插入或更新时   |

### 为什么不用 `num_map[complement] `直接查找？

因为 num_map[complement] 有一个副作用：

如果键不存在，会自动创建该键，并赋予默认值（0），这会改变哈希表的内容！

## 💡 补充

### 暴力解法参考：

```cpp
// 时间复杂度 O(n²)
vector<int> twoSumBruteForce(vector<int>& nums, int target) {
    for (int i = 0; i < nums.size(); i++) {
        for (int j = i + 1; j < nums.size(); j++) {
            if (nums[i] + nums[j] == target) {
                return {i, j};
            }
        }
    }
    return {};
}
```

---

### 在这道题的评论区里看到了个大佬的做的[可视化网页](https://chenqiwen.github.io/lc-1/)，安利一下
